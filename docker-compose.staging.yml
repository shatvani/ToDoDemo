networks:
  proxy:
    external: true

services:

  db:
    image: postgres:16-alpine
    container_name: todo-db-staging
    environment:
      POSTGRES_DB: tododb
      POSTGRES_USER: todo
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - todo-db-staging-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U todo -d tododb"]
      interval: 5s
      timeout: 5s
      retries: 5
    # Staging-ben az adatbázis nincs kívülre nyitva
    expose:
      - "5432"

  api:
    image: ghcr.io/shatvani/todo-ai-demo:${IMAGE_TAG:-develop}
    container_name: todo-api-staging
    environment:
      ASPNETCORE_ENVIRONMENT: Staging
      ConnectionStrings__DefaultConnection: "Server=db;Database=tododb;User Id=todo;Password=${DB_PASSWORD};TrustServerCertificate=True"
      TodoApi__BaseUrl: "http://localhost:8080"
    expose:
      - "8080"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - default
      - proxy

volumes:
  todo-db-staging-data:
